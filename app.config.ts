export default defineAppConfig({
      ui: {
            notifications: {
                  // Show toasts at the top left of the screen
                  position: 'top-0 start-0'
            }
      }
})
